# Unit Tests for SDSL-Minimal

This directory contains unit tests for the Configuration and Voxel structures.

## Running Tests

From this directory:

```bash
pytest -v
```

Or from the sdsl-minimal root:

```bash
pytest tests-minimal/ -v
```